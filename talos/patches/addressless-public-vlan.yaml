# This patch keeps public VLAN interfaces addressless.
# It is applied only to nodes that have eth1 connected to the public VLAN.
# eth1 is used for kube-vip Service VIPs; IPv6 is disabled on it.

machine:
  sysctls:
    net.ipv6.conf.eth1.disable_ipv6: "1"
