# This file aggregates platform configuration resources that depend on platform controllers.

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - external-secrets
  - clusterissuers
  - gateway
