# 2026-06-04

## 実施内容

- Gateway API CRD（v1.5.1）を vendor し Git 管理に追加した
- Cilium HelmRelease に `gatewayAPI.enabled`, `l2announcements.enabled` を追加した
- Cilium LB IPAM pool を追加した（163.220.236.73-76）
- CiliumL2AnnouncementPolicy を追加した（public VLAN, worker-only）
- shared public Gateway を追加した（HTTPS, cert-manager annotation, 163.220.236.73）
- external-dns に Gateway API source（gateway-httproute）を追加した
- runbook `3-flux-gitops-bootstrap.md` の構成図と reconcile コマンドを最新化
- `gateway-api-crds/README.md` を作成（CRD の生成元・再生成手順を明記）

## 判断

- Ingress Controller は導入せず、Cilium Gateway API を採用する
- Gateway は platform の責務、HTTPRoute は apps の責務
- L2 Announcement は public VLAN（eth1）、gateway worker（wk-01, wk-02）のみで行う
- control-plane は public ingress path から外す
- 163.220.236.73 を shared HTTPS Gateway に使う
- .74-.76 は将来の Gateway / L4 Service 用に予約

## 未完了

- worker への public VLAN NIC（eth1）追加（Proxmox / Talos config 適用）
- Gateway TLS の production 化（staging 検証後）
- monitoring / logging / backup
- 既存アプリ移行
