# 2026-06-27

## 実施内容

- secret delivery を 1Password Operator から External Secrets Operator へ移行する変更を確認した
- 旧 1Password Operator / OnePasswordItem の参照を Kubernetes manifests から外し、External Secrets Operator の HelmRelease / ClusterSecretStore / ExternalSecret 構成へ整理した
- External Secrets Operator bootstrap 用 service account token は SOPS 暗号化 Secret とし、Secret value は記録しない方針を確認した
- `cert-manager` / `external-dns` は専用 namespace に配置し、それぞれ External Secrets Operator が生成する Cloudflare token Secret を参照する構成にした
- `ClusterSecretStore` は namespace label によって `cert-manager` / `external-dns` からのみ利用できるようにした
- `apps` namespace は現時点では不要なため追加しないことにした
- 旧 1Password Operator 前提の README / runbook / security docs を External Secrets Operator 前提に更新した
- commit / push は行っていない

## 判断

- Secret value は 1Password、Kubernetes Secret の形は `ExternalSecret.spec.target.template` で Git 管理する
- SOPS で管理する Kubernetes Secret は、External Secrets Operator が 1Password を読むための bootstrap token に限定する
- `.env` template の smoke-test 用 namespace は、現時点では対象を増やすだけなので追加しない
