# 2026-06-03

## 実施内容

- `pull-age-key` を root Taskfile に移動し、talos Taskfile から責務を分離した
- `kubernetes/flux/Taskfile.yml` を作成し、`bootstrap-sops` task を追加した
- `gotk-sync.yaml` に SOPS decryption 設定を追加した
- `docs/bootstrap/gitops.md` を作成し、trust anchor の運用を明文化した
- `platform/` を `controllers/` と `config/` に分割した
- Flux Kustomization を `platform-controllers` / `platform-config` に分割し、`apps` の依存を `platform-config` に変更した
- `sources.yaml` に jetstack / external-dns / 1password の HelmRepository を追加した
- 1Password Operator の HelmRelease (`connect` 2.4.1, authMethod: service-account) を作成した
- SOPS 暗号化ルールを `kubernetes/.*/secrets/.*\.sops\.ya?ml$` に絞り、Secret manifest は `secrets/` ディレクトリに置く規約とした
- `docs/security/credential-handling.md` に `secrets/` ディレクトリ規約と SOPS 暗号化ルールを明文化した
- `kubernetes/README.md` を最新構成に更新した
- cert-manager の HelmRelease（v1.20.2, `crds.enabled: true`）を作成した
- external-dns の HelmRelease（v1.21.1, Cloudflare provider, `upsert-only`）を作成した
## 判断

- SOPS age key の pull 操作は root Taskfile に一本化し、重複定義を避けた
- `sops-age` Secret は Git に置かず、`task flux:bootstrap-sops` で手動適用する trust anchor とした
- `platform-controllers` と `platform-config` は適用順序のために分離。namespace は `platform` に統一

## 未完了

- ClusterIssuer / Certificate の実値設定（`<YOUR_EMAIL>`, `<YOUR_DOMAIN>`）
- Cloudflare API tokens の 1Password への保存
- smoke-test TLS 検証
