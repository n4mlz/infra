# 2026-06-26

## 実施内容

- OpenTelemetry Operator + Collector CR を軸とする可観測性基盤を構築した
- ディレクトリ/ファイル一覧は `kubernetes/platform/controllers/observability/README.md` および `kubernetes/platform/config/observability/README.md` に記載
- 以下のコンポーネントを追加:
  - MinIO (in-cluster S3 object storage, non-persistent)
  - kube-prometheus-stack (Prometheus / Grafana / Alertmanager)
  - Loki (community chart, log backend)
  - Tempo (trace backend)
  - OpenTelemetry Operator
  - OTel Collector Gateway (traces/logs pipeline)
  - OTel Collector Logs Agent (DaemonSet, filelog receiver)
  - Grafana datasources / ServiceMonitors / PrometheusRules / Istio telemetry
- Loki は grafana-community/helm-charts を使用 (grafana 公式 repo の Loki が GEL only に移行したため)
- `istiod` の `meshConfig.extensionProviders` に Istio tracing 用 `otel` provider を追加
- OTel Collector の `k8sattributes` processor 用 RBAC を追加
- `docs/architecture/observability.md` を追加し、リポジトリルート README からリンクした
- object-storage / observability の説明を platform 配下 README に追記
- OpenTelemetry Operator chart `0.117.0` は Helm repo index に存在することを確認したため、version は維持し README に appVersion を追記した
- `docs/architecture/observability.md` を監視基盤の正規ドキュメントとして再構成し、下位 README の重複説明を削減した

## 判断

- Metrics は Prometheus native scrape (ServiceMonitor/PodMonitor) のまま、Collector 経由にはしない
- Alloy は使わず OpenTelemetry Collector に統一
- 各 chart/image version は調査時点の最新安定版に pin

## 検証

- kustomize build は kubectl 非搭載環境で未実施
- helm template による各 chart の service naming は確認済み
  - Tempo chart: `releaseName: tempo` の場合、service 名は `<fullname>-<component>` パターン。fullname は release name と同じになるため `tempo-distributor`, `tempo-query-frontend` が正しい
- Git に secret の平文が含まれていないことを確認 (`grep -R` で key reference と env var 展開のみ)

## TODO

- [ ] 1Password に以下を登録する:
  - vault: Infra
  - Grafana admin credentials (admin-user / admin-password)
  - Alertmanager Discord webhook (webhook-url)
  - MinIO root credentials (MINIO_ROOT_USER / MINIO_ROOT_PASSWORD)
  - Observability object storage credentials (AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY)
- 1Password items は既存の `kubernetes/platform/config/onepassword-items/` に統合 (observability-items.yaml として追加)
- これに伴い `platform-observability-secrets` Kustomization は削除。`platform-object-storage` の依存先は `platform-config` に変更。
- [ ] cluster に apply 後、bucket 作成と telemetry 疎通を確認
- [ ] MinIO を StatefulSet/PVC に置き換え
- [ ] Grafana public exposure + Cloudflare Access 導入
- [ ] MinIO backup to external B2/R2
